# Core API Framework
fastapi==0.118.2
uvicorn==0.37.0
starlette==0.48.0
websockets==15.0.1
anyio==4.11.0
h11==0.16.0
httptools==0.6.4
watchfiles==1.1.0

# API Data Validation/Models
pydantic==2.12.0
pydantic-core==2.41.1

# Database/ORM (SQLAlchemy)
SQLAlchemy==2.0.44  # Or adjust to match your installed version: SQLAlchemy==2.0.x
greenlet==1.1.3 # Required by some SQLAlchemy versions

# Authentication and Utilities (based on common FastAPI practices)
python-jose==3.5.0
python-multipart==0.0.20
python-decouple==3.8
python-dotenv==1.1.1
passlib==1.7.4 # For password hashing

# Production Server (REQUIRED for your Dockerfile CMD)
gunicorn==21.2.0 # (Use a stable version matching your setup)

# Data Analysis/ML (Used by your recommender.py)
pandas==1.5.0 # Use a specific version for compatibility
# Note: You may need to add numpy, scipy, scikit-learn, etc., if your recommender.py uses them